import styled from 'styled-components'
import css from './css'

export const Container = styled.div([], {
  marginLeft: 'auto',
  marginRight: 'auto',
  padding: '32px',
  maxWidth: '768px'
}, css('LayoutContainer'))

export default Container
